apiVersion: v1
kind: Service
metadata:
  name: {{ include "traverse.fullname" . | default "traverse" }}
  labels:
    app: traverse
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: traverse
